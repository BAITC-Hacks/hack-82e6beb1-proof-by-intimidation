# Проверка версии 2 — 23.09.2026

Это журнал выполненных проверок, а не обещание отсутствия ошибок или оценка жюри.

## Автоматические проверки

- В новом Python-окружении `.venv-check` установлены только зависимости из `requirements.txt`; `pip check` не обнаружил конфликтов.
- Полный `pytest -q`: **67 passed**. Одно предупреждение стороннего Starlette о deprecated-псевдониме AnyIO; ошибок нет.
- `npm test` в `frontend/`: **21 passed**. Проверяются сохранение правок, явно очищенные поля, недействительность старого рейтинга, смена исходного текста, языки, сетевые ошибки и отсутствие повторной отправки изменений.
- Vite production build выполнен успешно. `npm audit`: **0 известных уязвимостей** на момент проверки, включая зависимости сборки.
- Локальный оценочный набор: 12 сценариев, 0 проваленных автоматических проверок. Этот запуск проверял **офлайн-контракт**, а не качество реального AI.
- `import agent.core` и `import streamlit` работают в прежнем окружении: первая версия сохранена.

## Проверка чистого исходного дерева

В игнорируемую папку `.qa/clean-source` скопированы только видимые Git исходные файлы. Не копировались `.env`, SQLite, `.git`, виртуальные окружения и `node_modules`.

На снимке перед последними небольшими проверками журнала/сети прошли 66 Python-тестов, 13 frontend-тестов, новая установка `npm ci` и сборка. Финальное дерево отдельно прошло 67 и 21 тест соответственно.

Из чистого дерева Uvicorn на временном порту 8001 успешно отдал `/api/health`, HTML, JavaScript и CSS. Новая SQLite содержала 5 задач, 5 команд, 5 черновиков и 5 предложений. Оценки учебных задач: 93, 89, 81, 43, 23. При пустых настройках ключа AI корректно обозначен как недоступный. Тестовый сервер остановлен.

Проверено на Windows, Python 3.13.15 и Node.js 24.19.0. Linux/macOS и буквальный fresh clone опубликованного коммита в этой сессии не запускались; использована чистая копия текущих исходников и заново установленное Python-окружение.

## Реальный браузерный сценарий

На `http://127.0.0.1:8000` с настоящим `gpt-4.1`, без подмены AI-ответа:

1. Выбран короткий библиотечный черновик. AI показал 5 вопросов по данным каталога, результату, приёмке, ограничениям и взаимодействию; рейтинг 15/100.
2. Подставлены явно помеченные синтетические ответы. AI вернул проверенные поля и рейтинг 100/100; повторный анализ занял 16,4 секунды. Оценки не зафиксированы и могут отличаться при следующем запуске.
3. Раскрыты основание оценки и журнал настоящих вызовов: рубрика, проверка 10 цитат, расчёт баллов, финальная проверка.
4. Без флажка подтверждения публикация недоступна. После ручного подтверждения карточка сохранена.
5. Отправлено учебное предложение, автор вручную выбрал команду и подтвердил учебный этап. Показаны +10 баллов; повторная форма начисления скрыта.
6. Страница перезагружена: задача, предложение, выбор и баллы сохранились.
7. Фильтр «Черновик» показал задачу с 23 баллами; подача предложения доступна, включая собственное название команды.
8. Проверены русская и казахская оболочки; desktop 1280×900 и phone 390×844. В мобильном представлении ширина документа совпадает с шириной viewport, горизонтального переполнения нет. В журнале браузера нет ошибок или предупреждений.

Дополнительные содержательные испытания AI описаны в [LIVE_EVAL.md](LIVE_EVAL.md).

## Что это не доказывает

- Нет нагрузочного теста, независимого аудита безопасности, пользовательского исследования или публичного деплоя.
- Cookie владельца — ограничение доступа в MVP, не полноценные аккаунты с восстановлением.
- Семантическая оценка AI может ошибаться. Проверка цитат гарантирует происхождение текста, не истинность фактов и не полноту извлечения.
- Демо-предложение и подтверждённый этап в локальной базе — синтетические действия проверки, не настоящая работа студенческой команды.
- Проверка ключеподобных строк не нашла секретов в новых исходниках; `.env` и рабочие базы исключены из Git.
